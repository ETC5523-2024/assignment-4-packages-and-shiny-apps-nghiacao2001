#' Generate Full Season Stats for a Team
#'
#' This function calculates the full season statistics for a given team in the English Premier League dataset.
#'
#' @param x A data frame of match data.
#' @param teamName The name of the team to calculate stats for.
#' @param resultColumn The column representing the match result.
#' @param resultPrefix Prefix for home and away goal columns (default is "FT" for full-time).
#'
#' @return A data frame with the team's season stats.
#' @export
#' @name full_season_stats
full_season_stats <- function(x, teamName, resultColumn = FTR, resultPrefix = FT) {
  # Function code
}
